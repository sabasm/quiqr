export const trackEvent = (eventName: string, properties?: Record<string, any>) => {
  // Implementation for tracking events (e.g., using Google Analytics)
  console.log(`Tracked event: ${eventName}`, properties);
};


